# План улучшений RAG (retrieval по строительным нормам)

Полная актуальная версия плана анализа и улучшений находится в файле:

**RAG_RETRIEVAL_IMPROVEMENT_PLAN.md**

(сохранена в корне проекта по запросу пользователя; синхронизируется с внутренней копией плана в сессии).

См. этот файл для:
- Полного анализа проблем retrieval
- Выявленных корневых причин (с примерами из metadata, кода, логов)
- Конкретного поэтапного плана (Фазы 0-4) с учётом ограничений слабого ноутбука (CPU, batch=2)
- Рекомендаций по чанкингу, предобработке, hybrid search, embeddings и т.д.

Файл обновляется при ревизиях плана.
